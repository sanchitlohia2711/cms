select * from country order by created_at DESC  limit 1;
select * from city order by created_at DESC limit 1;


select * from brand order by created_at DESC limit 1;
select * from vertical order by created_at DESC  limit 1;
select * from brand_vertical_mapping order by created_at DESC  limit 1;


select * from product order by created_at DESC limit 1;
select * from category order by created_at DESC limit 1;
select * from product_category_mapping order by created_at DESC limit 1;

select * from entity order by created_at DESC limit 1;
select * from event order by created_at DESC limit 1;
select * from person order by created_at DESC limit 1;
select * from event order by created_at DESC limit 1;